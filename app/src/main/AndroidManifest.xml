<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hotelqualitifinalproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HotelQualitiFinalProject">
        <!--
            <activity android:name=".Finished" />
        <activity
            android:name=".CheapestHotel"
            android:label="@string/title_activity_cheapest_hotel">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->
        <activity android:name=".EditProfile" />
        <activity android:name=".RegisterPage" />
        <activity android:name=".loginpage" />
        <activity android:name=".ReservationOptions" />
        <activity android:name=".CheapestHotel"/>
        <activity
            android:name=".ReservationScreenTwo"
            android:parentActivityName=".ReservationScreen" />
        <activity
            android:name=".ReservationScreen"
            android:parentActivityName=".DetailsActivity" />
        <activity
            android:name=".DetailsActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>